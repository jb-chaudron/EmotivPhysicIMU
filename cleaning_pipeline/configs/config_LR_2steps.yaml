experiment_name: xp2_cleaning_two_levels

data:
  artefact_path: XP1_ARTEFACTS
  eeg_path: XP1_EEG

preprocessing:
  physics_informed_artefacts: true

model:
  name: two_step_residual
  base_model: linear_regression
  first_features: lorentz
  residual_features: motion
  random_state: 42

report:
  output_dir: outputs