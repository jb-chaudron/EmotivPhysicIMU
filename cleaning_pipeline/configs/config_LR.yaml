experiment_name: xp1_cleaning

data:
  artefact_path: XP1_ARTEFACTS
  eeg_path: XP1_EEG

model:
  name: linear_regression
  channel_handling: per_channel
  random_state: 42

report:
  output_dir: outputs